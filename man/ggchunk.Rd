% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggchunk.R
\name{ggchunk}
\alias{ggchunk}
\title{Fold a ggplot axis into one continuous multi-segment figure}
\usage{
ggchunk(
  method = "auto",
  n_chunks = NULL,
  n_per_chunk = NULL,
  even = FALSE,
  nrow = NULL,
  ncol = NULL,
  threshold_col = 30,
  threshold_points = 10000,
  verbose = FALSE,
  show_indicator = FALSE,
  fixed_perpendicular = TRUE,
  ...
)
}
\arguments{
\item{method}{Folding strategy: \code{"auto"} (default), \code{"x"} or \code{"y"}.
(\code{"ncol"} is accepted as an alias of \code{"x"}; the grouping-based methods of
early drafts were removed -- use \code{facet_wrap()} for those.)}

\item{n_chunks}{Number of axis segments for continuous axes (\code{"x"} on
continuous data, \code{"y"}). Default 3.}

\item{n_per_chunk}{Number of discrete x levels per segment. Default 20.}

\item{even}{Continuous axes only: cut at quantiles so every segment holds
roughly the same number of observations instead of covering equal axis
ranges. Default \code{FALSE} (equal-width segments).}

\item{nrow, ncol}{Grid of segments in the single figure. \code{NULL} (default)
puts x-axis segments into rows and y-axis segments into columns.}

\item{threshold_col}{\code{"auto"}: truncate the x axis when the discrete x
variable has more than this many levels.}

\item{threshold_points}{\code{"auto"}: truncate the continuous x axis when the
data has more than this many rows.}

\item{verbose}{Print the detection result and segment statistics.}

\item{show_indicator}{Add a small continuation arrow to every non-final
segment. The arrow is drawn in the gutter between segments, never inside
a data panel: it points downward for x-axis rows and rightward for
y-axis columns. It is omitted when an explicit multi-panel layout is
supplied, so that user-specified dimensions are not changed. Default
\code{FALSE}; set to \code{TRUE} to show the continuation cue.}

\item{fixed_perpendicular}{Fix the non-chunked axis to the full plot range
across all segments. For \code{method = "x"}, this fixes y; for \code{method = "y"},
this fixes x. Default \code{TRUE}; set to \code{FALSE} for per-segment ranges.}

\item{...}{Unused; a warning is issued for unknown arguments (catches
typos).}
}
\value{
When used with \code{+}: the ggplot object carrying a \code{ggchunk} class
and the truncation specification (an ordinary ggplot if no truncation is
needed). Calling \code{ggchunk()} on its own returns the internal
specification object.
}
\description{
\code{ggchunk()} is added to a ggplot pipeline with \code{+}. It \strong{folds the x or y
axis into several consecutive segments} and draws all segments in \strong{one
figure}: x-axis segments become rows and y-axis segments become columns.
The segments keep the perpendicular scale, touch edge-to-edge, and share
figure-level labels and legends where appropriate.
}
\details{
Methods:
\itemize{
\item \code{"x"}: truncate the x axis. Discrete x: \code{n_per_chunk} levels per segment.
Continuous x: the x range is cut into \code{n_chunks} equal-width segments
(or equal-density segments with \code{even = TRUE}), so a dense strip along x
is folded into readable strips.
\item \code{"y"}: truncate the y axis (segments are arranged in columns and share
the x axis). Continuous y only.
\item \code{"auto"} (default): a discrete x variable with more than \code{threshold_col}
levels triggers \code{"x"}; a continuous x with more than \code{threshold_points}
rows triggers \code{"x"} on the x range. Small plots pass through unchanged.
}

Layout: x-axis segments are stacked into \strong{rows} and y-axis segments are
arranged into \strong{columns} by default; \code{nrow}/\code{ncol} can be set explicitly to
arrange them differently. All segments always render in \strong{one figure} --
there are no pages.

The returned object is still a full ggplot (with the chunking spec
attached), so \code{theme()}, \code{labs()}, scales and geoms keep working before or
after \code{+ ggchunk()}. When no explicit theme is supplied, ggchunk uses a
restrained 14 pt text baseline with 12 pt axis text; user-supplied themes
are preserved. Use the usual ggplot2 theme, text-size and font-family
settings for the target journal. When patchwork is installed, the figure is
a patchwork object and
\code{+ patchwork::plot_layout()} / \code{+ patchwork::plot_annotation()} style it.

Accessors: \code{\link[=n_chunks]{n_chunks()}}, \code{\link[=chunk_info]{chunk_info()}}, \code{\link[=chunk_plot]{chunk_plot()}}, \code{\link[=ggsave_chunk]{ggsave_chunk()}}.
}
\examples{
library(ggplot2)

# 60 x levels: fold the x axis into 3 segments of 20, ONE figure
df <- data.frame(
  variable = factor(rep(sprintf("v\%02d", seq_len(60)), each = 10)),
  value = rnorm(600)
)
p <- ggplot(df, aes(variable, value)) +
  geom_boxplot() +
  ggchunk(method = "x", n_per_chunk = 20)
p
n_chunks(p)
chunk_info(p)

# explicit 2 x 2 folding of a dense scatter
p2 <- ggplot(diamonds_sample, aes(carat, price)) +
  geom_point(alpha = 0.3) +
  ggchunk(method = "x", n_chunks = 4, nrow = 2, ncol = 2)
}
