% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{chunk-accessors}
\alias{chunk-accessors}
\alias{print.ggchunk}
\alias{plot.ggchunk}
\alias{chunk_plot}
\alias{n_chunks}
\alias{chunk_info}
\title{Print, plot and inspect a ggchunk object}
\usage{
\method{print}{ggchunk}(x, ...)

\method{plot}{ggchunk}(x, ...)

chunk_plot(x, i = NULL)

n_chunks(x)

chunk_info(x)
}
\arguments{
\item{x}{A \code{ggchunk} object (a ggplot carrying a truncation spec).}

\item{...}{Passed on to the print methods of the figure objects.}

\item{i}{Optional segment index for \code{chunk_plot()}.}
}
\value{
\code{chunk_info()} returns (invisibly) a data frame with one row per
segment; \code{chunk_plot()} returns the figure (or segment) object;
\code{n_chunks()} returns an integer; \code{print()}/\code{plot()} return the object
invisibly.
}
\description{
\code{print()} / \code{plot()} render the single figure holding all axis segments.
\code{chunk_plot()} returns the figure object itself (a ggplot for a single
segment, a patchwork composition when patchwork is installed, otherwise a
\code{ggchunk_page}); with an index it returns one raw segment plot.
\code{chunk_info()} summarises the segments and \code{n_chunks()} counts them.
}
\examples{
library(ggplot2)
df <- data.frame(
  variable = factor(rep(sprintf("v\%02d", seq_len(40)), each = 5)),
  value = rnorm(200)
)
p <- ggplot(df, aes(variable, value)) +
  geom_boxplot() +
  ggchunk(method = "x", n_per_chunk = 10)
n_chunks(p)
info <- chunk_info(p)
fig <- chunk_plot(p)    # the whole figure (patchwork if installed)
seg2 <- chunk_plot(p, 2) # second segment as a standalone ggplot
}
