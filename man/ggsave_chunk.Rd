% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsave-chunk.R
\name{ggsave_chunk}
\alias{ggsave_chunk}
\title{Save a ggchunk figure to a file}
\usage{
ggsave_chunk(
  x,
  filename,
  device = NULL,
  path = NULL,
  width = NA,
  height = NA,
  units = c("in", "cm", "mm", "px"),
  dpi = 300,
  limitsize = TRUE,
  bg = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{ggchunk} object.}

\item{filename}{File name; the extension selects the device unless
\code{device} is given.}

\item{device}{Device name (\code{"pdf"}, \code{"cairo_pdf"}, \code{"png"}, ...); derived
from \code{filename} when \code{NULL}.}

\item{path}{Directory in which to save; \code{NULL} (default) uses the working
directory.}

\item{width, height}{Figure dimensions; \code{NA} uses the device default.}

\item{units}{One of \code{"in"}, \code{"cm"}, \code{"mm"}, \code{"px"}.}

\item{dpi}{Resolution, used for \code{"px"} units and raster devices.}

\item{limitsize}{Passed to ggplot2 where applicable.}

\item{bg}{Background colour where the device supports it.}

\item{...}{Passed on to the device function.}
}
\value{
Invisibly, the file path.
}
\description{
Renders the complete figure -- every axis segment in one image -- to a
single PDF or raster/vector file. On ggplot2 older than 4.0, \code{ggsave()}
also works directly on \code{ggchunk} objects via S3 dispatch.
}
\examples{
\donttest{
library(ggplot2)
df <- data.frame(
  variable = factor(rep(sprintf("v\%02d", seq_len(40)), each = 5)),
  value = rnorm(200)
)
p <- ggplot(df, aes(variable, value)) +
  geom_boxplot() +
  ggchunk(method = "x", n_per_chunk = 10)
ggsave_chunk(p, tempfile(fileext = ".pdf"), width = 12, height = 4)
}
}
